[production]
# Replace this host with the public IP or DNS name from the Terraform VM output.
# Example:
# app ansible_host=203.0.113.10 ansible_user=ubuntu
app ansible_host=CHANGE_ME ansible_user=ubuntu
